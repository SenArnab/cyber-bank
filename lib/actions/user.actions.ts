'use server';

export const signIn = async () => {
    try {
        // MutationObserver / database / make fetch
    } catch (error) {
        console.error('Error: ', error)
    }
}

export const signUp = async (userData : SignUpParams) => {
    try {
        // Appwrite user
    } catch (error) {
        console.error('Error: ', error)
    }
}